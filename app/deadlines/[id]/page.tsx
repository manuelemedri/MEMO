'use client';import Link from 'next/link'; import { useParams } from 'next/navigation';import { useAppStore } from '@/stores/useAppStore';
export default function Detail(){const {id}=useParams<{id:string}>();const {deadlines,markPaid}=useAppStore();const d=deadlines.find(x=>x.id===id); if(!d) return null; return <div className='space-y-3'><h1 className='text-2xl font-bold'>Dettaglio scadenza</h1><div className='card'>{d.documentUrl}</div><div className='card'><p className='font-semibold'>{d.title}</p><p>{d.provider}</p><p className='text-2xl font-bold'>{d.amount.toFixed(2)} €</p><p>Scadenza {d.dueDate}</p><p>Stato: {d.status}</p></div><button onClick={()=>markPaid(d.id)} className='w-full py-3 bg-blue-600 text-white rounded-2xl'>Segna come pagata</button><Link href='/promemoria' className='block text-center py-3 bg-white rounded-2xl border'>Modifica promemoria</Link></div>}
