'use client';import { useAppStore } from '@/stores/useAppStore';const items=['Gmail','Outlook','Google Calendar','Apple Calendar','WhatsApp','Documenti'];
export default function I(){const {integrations,toggleIntegration}=useAppStore();return <div><h1 className='text-2xl font-bold'>Collega i tuoi canali</h1><p className='text-slate-500'>Connetti le app che usi ogni giorno.</p><div className='space-y-2 mt-3'>{items.map(i=><div key={i} className='card flex justify-between'><div><p className='font-medium'>{i}</p><p className='text-sm text-slate-500'>Integrazione pronta per API future</p></div><button onClick={()=>toggleIntegration(i)} className={`px-3 py-1 rounded-xl ${integrations[i]?'bg-green-100 text-green-700':'bg-blue-600 text-white'}`}>{integrations[i]?'Connesso':'Connetti'}</button></div>)}</div></div>}
