'use client';import { useState } from 'react';import { useAppStore } from '@/stores/useAppStore';export default function S(){const {shopping,addShopping,toggleShopping}=useAppStore();const [name,setName]=useState('');return <div><h1 className='text-2xl font-bold'>Lista spesa</h1><div className='card mt-3 flex gap-2'><input className='flex-1' placeholder='Aggiungi un elemento…' value={name} onChange={e=>setName(e.target.value)}/><button onClick={()=>{if(name.trim())addShopping(name),setName('')}} className='bg-blue-600 text-white px-3 rounded-xl'>Aggiungi</button></div><div className='space-y-2 mt-3'>{shopping.map(i=><label key={i.id} className='card flex gap-2'><input type='checkbox' checked={i.completed} onChange={()=>toggleShopping(i.id)}/><span className={i.completed?'line-through text-slate-400':''}>{i.name} <small className='text-slate-400'>({i.category})</small></span></label>)}</div></div>}
